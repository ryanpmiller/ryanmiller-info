{
  "name": "ryanmiller-info",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "repository": "https://github.com/ryanpmiller/ryanmiller-info",
  "devDependencies": {},
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && NODE_ENV=production webpack",
    "start": "npm run clean && webpack && node server.js",
    "lint": "./node_modules/.bin/eslint --ext .js --ext .jsx .",
    "lint-fix": "./node_modules/.bin/eslint --fix --ext .js --ext .jsx ."
  }
}
